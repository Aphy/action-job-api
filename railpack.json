{
  "$schema": "https://schema.railpack.com",

  "steps": {
    "install": {
      "commands": [
        { "image": "docker.io/library/composer:2", "cmd": "composer install --working-dir=/app/api --no-dev --prefer-dist --no-interaction --optimize-autoloader --no-scripts -vvv" }
      ]
    },

    "build": {
      "inputs": [
        { "step": "install" }
      ],
      "commands": []
    },

    "migrate": {
      "inputs": [
        { "step": "build" }
      ],
      "commands": [
        { "image": "ghcr.io/railwayapp/railpack-runtime:latest", "cmd": "sh -lc 'for i in $(seq 1 60); do php /app/api/bin/console doctrine:database:create --if-not-exists --no-interaction >/dev/null 2>&1 && exit 0 || { echo \"DB not ready… ($i)\"; sleep 2; }; done; echo \"DB not ready after timeout\"; exit 1'" },
        { "image": "ghcr.io/railwayapp/railpack-runtime:latest", "cmd": "php /app/api/bin/console doctrine:migrations:migrate --no-interaction --allow-no-migration" },
        { "image": "ghcr.io/railwayapp/railpack-runtime:latest", "cmd": "php /app/api/bin/console cache:warmup --env=prod" }
      ]
    }
  },

  "deploy": {
    "base": { "image": "ghcr.io/railwayapp/railpack-runtime:latest" },
    "inputs": [
      { "step": "migrate" }
    ],
    "variables": {
      "APP_ENV": "prod",
      "RAILPACK_PHP_ROOT_DIR": "api/public"
    },
    "startCommand": "/start-container.sh"
  }
}
